<%- include('include/_header') %>

<main id="site-main">
    <div class="container">
        <div class="box-nav d-flex justify-center">
            <a href="/add-item" class="border-shadow no-color">
                <span class=""><i class="fas fa-pallet"></i> New Item</span>
            </a>
            <a href="/export" class="border-shadow no-color">
                <span class=""><i class="fas fa-download"></i> Export to CSV</span> 
            </a>
        </div>

        <form action="/" method="POST">
            <table class="table">
                <thead class="thead-dark">
                    <tr>
                        <th>ID</th>
                        <th>Item Name</th>
                        <th>Type</th>
                        <th>Status</th>
                        <th>Location</th>
                        <th>Received Date</th>
                        <th>Shipped Date</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    <%- include('include/_show') %>
                </tbody>
            </table>
        </form>
    </div>
</main>

<%- include('include/_footer') %>